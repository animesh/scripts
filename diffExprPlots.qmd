---
title: "diffExpr"
format: html
server: shiny
---

```{r data}
#| context: setup
#| include: true
inpF<-"L:\\promec\\TIMSTOF\\LARS\\2023\\230222 Katja\\Protein comparison ADN3 vs AD for figure 1.csv"
data<-read.csv(inpF,header=TRUE,sep=",",row.names=1)
#data<-as.numeric(data)
#summary(data)
dataNAR<-data
dataNAR[is.na(dataNAR)]<-0
#summary(dataNAR)
```

data select:

```{r select}
sliderInput("sample", "Number of samples", min = 1, max = ncol(data), value = 1)
plotOutput("distPlot")
```

```{r plot}
#| context: server
output$distPlot <- renderPlot({
   x <- dataNAR[,input$sample]
   hist(x, col = 'darkgray', border = 'white',xlab = 'log2LFQ',main = inpF)
})
```

